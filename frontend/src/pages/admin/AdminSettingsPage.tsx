import { useNavigate } from 'react-router';
import { ArrowLeft, Settings, User } from 'lucide-react';
import { useSelector } from 'react-redux';
import type { RootState } from '../../redux/app/store';
export default function AdminSettingsPage(){const navigate=useNavigate();const user=useSelector((s:RootState)=>s.auth.user);return <div className="min-h-screen bg-background p-4 sm:p-6"><div className="mx-auto max-w-5xl"><button onClick={()=>navigate('/admin/dashboard')} className="mb-5 flex items-center gap-2 text-sm font-semibold"><ArrowLeft size={17}/> Admin Dashboard</button><div className="rounded-2xl bg-surface p-6 shadow-sm"><div className="flex items-center gap-3"><Settings size={23}/><h1 className="text-xl font-bold">Admin Settings</h1></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><div className="rounded-xl border border-surface-border p-4"><User size={20}/><p className="mt-3 text-sm text-text-secondary">Administrator</p><p className="font-semibold">{user?.name||'—'}</p><p className="text-sm">{user?.email||'—'}</p></div><div className="rounded-xl border border-surface-border p-4"><p className="text-sm text-text-secondary">Role</p><p className="mt-1 font-semibold">{user?.role||'ADMIN'}</p><p className="mt-3 text-xs text-text-secondary">System configuration remains controlled by the existing backend.</p></div></div></div></div></div>}
